import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles.js';

const Post = (props) => {
    return (
        <View>
            {/* Image */}
            {/* Bed & Bedroom */}
            {/* type & description */}
            {/* old price & new price */}
            {/* total price */}
        </View>
    );
};

export default Post;